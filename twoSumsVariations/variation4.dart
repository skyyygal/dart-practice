/* Variation 4 – Two Sum Less Than Target
🔹 Find the maximum sum of two numbers less than target.
Example:

nums = [34, 23, 1, 24, 75, 33, 54, 8]
target = 60
Output = 58 // from 34 + 24
💡 Key twist: Two pointers but track max sum instead of exact match. */
